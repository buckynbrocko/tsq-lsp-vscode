// import { DELETE_ME_CheckableType } from '../src/CheckableType';

describe('CheckableType', () => {
    test.each([''])('instantiation', name => {
        expect(true).toBeTruthy();
        // let named = RUST.filter(isNamed);
        // for (let type of named) {
        //     let name = type.type;
        //     let checkable = new DELETE_ME_CheckableType(type, RUST);
        //     checkable.allTypeNames;
        // }
    });
});
